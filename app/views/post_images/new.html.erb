<h1>画像投稿フォーム</h1>
<%= form_with model: @post_image, local:true do |f| %>
<!--form_withはrails 5.1以上-->
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <!--refileの文法の決まりで_idは要らない決まり-->
  <!--指定するフィールド名は、image_idから_idを除いた名前（image）を指定-->
  <h4>ショップ名</h4>
  <%= f.text_field :shop_name %>
  <!--カラムにユーザーが入れるものを保存するイメージ-->
  <h4>説明</h4>
  <%= f.text_area :caption %>
  <%= f.submit '投稿' %>
<% end %>